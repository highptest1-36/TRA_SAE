# TRA-SAE /predict API — inference + serving deps.
# (Versions pinned to what was validated on the A100/Colab box.)
torch>=2.4
transformers>=4.46
peft>=0.11
datasets>=2.19
accelerate>=0.30
scikit-learn>=1.3
scipy>=1.10
numpy>=1.24
fastapi>=0.110
uvicorn>=0.27
# Optional (public URL): cloudflared binary (not a pip package) — see run_server.sh
